from notcher import *
from layout import *
from slicer import *
from flexurizer import *
def make_cuts():
	# slices = slice(file)
	# notches = notch(slices)
	# flexures = flexurize(slices)
	# for slice in slices:
	#	slice_notches = notches[slice]
	# 	slice_flexures = flexures[slice]
	# 	slice.segments.append(slice_notches)
	# 	slice.segments.append(slice_flexures)
	# return layout(slices)